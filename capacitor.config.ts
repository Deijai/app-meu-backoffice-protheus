import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-meu-backoffice-protheus',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
